spring:
  datasource:
    url: jdbc:mysql://localhost:3307/movielibrary?useSSL=false&allowPublicKeyRetrieval=true
    username: szymon
    password: szymon
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect