spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movielibrary?useSSL=false
    username: root
    password: szymon
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect